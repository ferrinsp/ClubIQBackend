services:
  postgres:
    image: postgres:15
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: clubiq
      POSTGRES_USER: clubiq
      POSTGRES_PASSWORD: clubiq_dev
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
